/**
 * Sensors can expose arbitrary information from cells' surroundings to
 * the cell, as floating point numbers from 0 to 1
 */
export interface Sensors {
  a: number;
  b: number;
  c: number;
  d: number;
}
